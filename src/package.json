{
  "name": "potniq",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist && echo 'Done.'",
    "dev": "bun  --watch index.ts",
    "build": "bun build ./index.ts --target=bun-linux-x64-baseline --compile --outfile ./dist/potniq --minify"
  },
  "bin": {
    "potniq": "./dist/potniq"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "prettier": "^3.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "path-parser": "^6.1.0"
  }
}